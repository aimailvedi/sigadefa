# SIGADEFA
## Инструкция по работе, ошибкам и взаимодействию с ИИ

Этот документ фиксирует:
- практические шаги работы с проектом,
- типовые технические ошибки,
- реальные кейсы их решения,
- особенности взаимодействия с ИИ-ассистентом.

---

## 1. Общие принципы работы

- Рабочая директория: D:\GIT\sigadefa
- Один логический шаг = один PowerShell-блок = один commit.
- После каждого значимого блока:
  - git add .
  - git commit -m "...",
  - git push.

---

## 2. Первый акустический прототип (sigadefa_first_sound.wav)

**Симптомы проблем:**
- файл WAV не создавался;
- Python-скрипт выполнялся без видимых ошибок;
- в директории проекта не было файла sigadefa_first_sound.wav.

**Найденные причины:**
- не был установлен пакет 
umpy;
- некорректный импорт FrequencyModel;
- в Python-файлы попали экранированные кавычки \" и фрагменты PowerShell.

**Решение (кратко):**
- python -m pip install numpy;
- пересобрать udio_export.py и requency_model.py как чистый Python;
- проверять содержимое файлов через Get-Content.

**Итог:**
- файл sigadefa_first_sound.wav успешно создан и прослушан;
- Фаза 2 (акустический прототип) считается технически выполненной на базовом уровне.

---

## 3. Замечания по взаимодействию с ИИ

- Давать ИИ чёткие, изолированные блоки команд.
- Не смешивать в одном блоке несколько несвязанных задач.
- Всегда проверять сгенерированные файлы через Get-Content.
- При генерации Python-кода через PowerShell удобно использовать один here-string на файл.

Документ дополняется по мере развития проекта.
